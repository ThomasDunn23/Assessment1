#include <stdio.h>

int main()
{
    char EncriptedMessage[99], chr ;// Announcing Array and Message
    int x, shift  

    printf("Enter Message to Encript: %c\n", ) ; // Printing useable interface
    scanf ("%c" , &chr);
    printf("Enter Number of Shifts: %d\n" , shift) // User enters how many times they want to shift the cipher 
    scanf ("%d" , &shift)
    for ()



 }
 
